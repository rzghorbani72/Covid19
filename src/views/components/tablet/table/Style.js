import {makeStyles} from "@material-ui/core/styles";

export const useRowStyles = makeStyles({
    root: {
        flexGrow: 1,
    },
    paper: {
        padding: 2,
        textAlign: 'center',
    },
});